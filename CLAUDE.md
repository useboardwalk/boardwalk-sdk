@AGENTS.md

## Claude Code

`AGENTS.md` above is the single source of truth for this repo and is shared with the other
coding agents. Put repo guidance there, not here.

Before writing, reviewing or refactoring code here, invoke the
`andrej-karpathy-skills:karpathy-guidelines` skill if it resolves. It ships from the
`karpathy-skills` marketplace at user scope, not from this repo, so a fresh checkout may not
have it — in that case follow the principles as paraphrased in `AGENTS.md` instead of
treating this as a blocker.
